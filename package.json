{
  "name": "hh-challenge",
  "version": "0.0.1",
  "description": "Helpful Human Interview Challenge",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "seed": "node seeders/swatches.js",
    "heroku-postbuild":"npm run seed && cd client && npm install --only=dev && npm install && npm run build"
  },
  "author": "@shullmb",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^6.1.0",
    "express": "^4.16.4",
    "mongoose": "^5.3.14",
    "path": "^0.12.7"
  }
}
